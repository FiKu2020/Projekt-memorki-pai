import { useState } from "react";
import PropTypes from "prop-types";
const Flashcards = (props) => {
  const { link, image, color } = props;
  return (<div></div>)
}
Flashcards.propTypes = {};
export default Flashcards;
